---
name: publish-article
description: ネタ（URL・下書きファイル・チャットで渡された話題）から、本人になりきって記事とX投稿を書き上げ、配信先（個人ブログ / Zenn / X / note）を判断して公開まで実行する。Obsidianの人格ノートを読んでから書くので、AIが書いた感じにならない。ユーザーが「この記事書いて」「ネタあるんだけど」「これで記事作って」とURLやメモを渡してきたとき、記事の公開・配信・クロスポストの話をしているとき、ZennやXやnoteへの投稿を頼まれたときは必ずこのスキルを使う。「記事にする」「発信する」「投稿して」といった言い方でも、明示的にブログと言われなくても使う。
---

# publish-article

ネタを受け取って、**本人の声で**記事を書き、どこに出すかを決めて、公開まで運ぶ。

## 前提となる構造

**記事のマスターは Obsidian。** ブログリポジトリはその同期先にすぎない。

```
Obsidian /個人/発信/記事/公開済み/     ← 記事の正本。ここに書く
        ↓  npm run sync:obsidian （rsync --delete）
yuki-dev-blog/src/content/blog/        ← 同期先
        ↓
   ブログ / Zenn / X / note
```

`sync-obsidian.sh` は `rsync --delete` で同期する。つまり **Obsidian に無い記事はブログから消える。**
ブログ側に直接書いて終わりにすると、次の同期で消滅する。必ず Obsidian に書く。

## 全体の流れ

```
ネタ → ⓪人格の読み込み → ①事実確認 → ②タイプ判定 → ③執筆 → ④X投稿の執筆 → ⑤生成物の提示
                                                                            ↓
                                                                    【ユーザー承認】← 止まるのはここだけ
                                                                            ↓
        ⑥Obsidianへ配置 → ⑦ブログ同期・公開 → ⑧デプロイ待ち → ⑨Zenn → ⑩X → ⑪note → ⑫state.jsonコミット
```

**承認前に外向きの操作は一切しない。** 承認後は最後まで走らせる。
途中で失敗したら、そこで止めて何が起きたかを報告する。黙って次に進まない。

## ⓪ 人格の読み込み（省略しない）

`references/persona.md` を読み、そこに挙がっている Obsidian のノートを実際に読む。

これを飛ばすと「よくできたAIの記事」になる。本人の発信として出す以上、ここが一番重要。
**人格の定義は Obsidian が正本**なので、スキル内の写しではなく必ず現物を読む。

## ① 事実確認

ネタがニュースやURLなら、**一次情報を複数ソースで裏取りする**。WebSearch と WebFetch を使う。
渡されたURL1本だけを信じて書かない。日付・数字・固有名詞は特に間違えやすい。

裏が取れなかったことは書かない。推測は本文中で「公開情報からの推測を含む」と明示する。
特定の企業や個人の内部事情を断定しない。設計論・一般論に昇華させる。

**本人の経歴や実績の数字は捏造しない。** Obsidian に書いてある実数だけを使う。

## ② 記事タイプの判定

`references/routing.md` を読んで A〜D を決める。ここで配信先がほぼ決まる。

- **A. 技術解説・設計論・ニュース考察** → ブログ + Zenn + X
- **B. 個人開発の裏側・プロダクト開発記** → ブログ + note + X
- **C. 速報・短い気づき** → X のみ（記事にしない）
- **D. キャリア・お金・働き方** → note + ブログ

## ③ 記事の執筆

`references/writing.md` を読んでから書く。

記事の声は **ですます調・一人称・実体験ベース**。X の常体とは別物なので混ぜない。
既定は「コード例なし・非エンジニアにも分かる」。技術的な深さは落とさず、例え話で伝える。

**書く場所**: `Obsidian /個人/発信/記事/下書き/YYYYMMDD-<英語スラッグ>.md`

承認前は `下書き` に置く。承認後に `公開済み` へ移す。こうすると、承認前の記事が
同期で誤ってブログに出ることがない（`sync-obsidian.sh` は `公開済み` だけを見る）。

## ④ X 投稿の執筆（自動生成に任せない）

**スレッドは本人の声で書き下ろす。** 見出しから機械的に生成した文面は必ず整いすぎて、AI臭が出る。

`references/persona.md` の「本人の声のサンプル」と、Obsidian の直近の投稿案ログを読んでから書く。
特に効くのは:

- 1行目は外の話から入る。自分の話は2行目以降
- 具体的な数字を1つだけ
- 常体。絵文字なし
- 推量でフェードアウトしない
- 締めは独白の問いか、事実の言い切り

書けたら frontmatter の `x.thread` に配列で入れる。これで自動生成が上書きされる。

```yaml
x:
  thread:
    - "1投稿目。外の話から入って、自分の数字で殴り返す"
    - "2投稿目。記事の核になる一点だけ"
    - "全文はこちら\nhttps://..."
```

最終投稿にURLを置く。**理由は2つ**: 1投稿目にリンクがあると伸びにくい。そしてURL入りポストは
課金が$0.20と高い（リンクなしは$0.015）ので、1件に集約するとコストも下がる。

## ⑤ frontmatter を組み立てて提示（ここで止まる）

キーの有無がそのまま配信先になる。

```yaml
---
title: "..."
description: "..."           # 検索結果とOGPに出る。1〜2文
pubDate: "YYYY-MM-DD"
heroImage: "/images/hero/YYYYMMDD-<スラッグ>.webp"
tags: ["..."]
categories: ["Development"]
author: "Yuki"
zenn:                        # ← あれば Zenn に配信
  slug: "..."
  emoji: "📦"
  type: "tech"
  topics: ["...", "..."]
  published: true
x:                           # ← あれば X に配信
  slug: "..."
  mode: "digest"
  thread: ["...", "...", "..."]   # 本人の声で書いたもの
  article_enabled: false          # 既定。代表作だけ true
note:                        # ← あれば note に配信
  tags: ["個人開発"]
---
```

`x.article_enabled` は通常 `false`。X Articles はタイムラインに流れず「記事」タブにしか出ないため、
到達はスレッドが担う。代表作だけ `true` にする。判断基準は `references/routing.md`。

提示するもの:

1. **記事タイプと配信先**（なぜそう判断したかを1〜2文で）
2. **記事本文**（`下書き` に書いた上で、要点と構成を示す）
3. **Xのスレッド全文**（実際に投稿される文面そのもの）
4. **公開ルールのチェック結果**（`自分/セキュリティと公開ルール.md` のリストを実際に当てた結果）
5. **確認してほしい点**（事実の不確かな箇所、一人称で書いた実感の部分、画像の要否）

ヒーロー画像はユーザーが用意する。無い場合は想定パスを書いておき、承認時に依頼する。

生成だけならコストも外部送信も発生しないので、ここまでは必ず実行してから見せる。

```bash
npm run sync:x     # スレッドと content_state の生成（ネットワークなし）
npm run sync:note  # note に出す場合
```

## ⑥〜⑫ 公開

承認を得たら `references/publishing.md` の手順に従う。**順番に意味がある。**
ブログが公開されデプロイが完了する前に X へ投稿すると、CTAリンクが404の記事を出すことになる。

## よくある失敗

過去に実際に起きたもの。

- **ブログ側に直接記事を書いた** → `sync:obsidian` の `--delete` で消える。記事は Obsidian に書く
- **Xのスレッドを自動生成のまま投稿した** → 見出しの番号列挙＋絵文字で、本人のルール違反かつAI臭
- **curl で x.com の200を見て「投稿成功」と判断した** → SPAなので常に200。確認は必ずブラウザで
- **ブログのデプロイを待たずにXへ投稿した** → CTAリンクが404だった
- **`.x/state.json` をコミットし忘れた** → 次回に二重投稿する
- **X Articles が「投稿されていない」ように見える** → 「記事」タブにあるだけ。仕様

## 参照ファイル

- `references/persona.md` — **執筆前に必ず読む。** Obsidianのどのノートを読むか、本人の声の特徴
- `references/routing.md` — 配信先の判断基準
- `references/writing.md` — 記事の文体・構成・タイトルの付け方
- `references/publishing.md` — 公開手順の詳細とコマンド
