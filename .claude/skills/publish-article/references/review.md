# 振り返りと学習

ユーザーは「最近全くチェックしていない」と言っている。**誰も見ていない前提で、学習が自動で回る必要がある。**
そのぶん、記録を残さないまま進むことが一番のリスクになる。

## 既にある仕組みには手を出さない

SNS側（X / Threads の日次投稿）には、すでに完成度の高い学習ループが動いている。

| ファイル | 役割 |
| --- | --- |
| `SNS/99_システム/実績/投稿実績.csv` | 実測データ。いいね・返信・インプ |
| `SNS/01_戦略/伸びる投稿研究.md` | 仮説（H番号）→ 検証 → 確定/棄却。日次追記 |
| `SNS/99_システム/実行記録/` | スロットごとの実行記録 |
| `SNS/03_レビュー/週次レポート/` | 週次のまとめ |

**このループを置き換えたり、並行して別のものを作ったりしない。** データが二重管理になって両方が腐る。
記事配信で X に投稿したときは、**同じ場所に記録を足す**。

## 記事を公開したら必ず記録する（⑫の実体）

### 1. X に投稿したものを SNS 側の記録へ

```bash
V="/Users/yuki/Library/Mobile Documents/iCloud~md~obsidian/Documents/YukiKawabata /個人"
LEDGER="$V/SNS/99_システム/投稿済み台帳/投稿済みX_ID.txt"
```

投稿した全ポストのURLを台帳へ追記する（タブ区切り: `URL\tclaude\tISO時刻`）。
台帳は**追記専用**。並べ替えも削除もしない。

`SNS/99_システム/実績/投稿実績.csv` にも行を足す。スロットは `記事配信` とし、
19:10の自動投稿と区別できるようにする。型は `記事告知スレッド(n/m)` の形で書く。

これをやらないと、SNS側の学習ループは「その日に何が投稿されたか」を取り違える。

### 2. 記事側の実績へ

`個人/発信/記事/実績/記事実績.csv` に1行足す。公開直後は数値が空でよい。

### 3. 記事研究ノートへ

`個人/発信/記事/記事研究.md` の研究ログに、その日の実測と学びを追記する。
**仮説（A番号）を立てて、次に何を試すかまで書く。** ここが学習の本体。

## 週次レビュー（記事側）

SNS側は日次の学習スロットが回っているので、記事側は週次で足りる。

1. `記事実績.csv` の数値を埋める
   - **Zennのいいね**: 記事ページをブラウザで開いて読む
   - **Xのインプ・いいね**: プロフィールから該当スレッドを開いて読む
   - **note のスキ**: note の記事ページ
   - **ブログPV**: 現状は計測手段がない（後述）
2. `記事研究.md` の「検証中の仮説」を更新する
   - 支持されたら「確定パターン集」へ昇格
   - 反証されたら「棄却した仮説」へ移す。**理由を書く**
3. 「今の指針（最大5行）」を書き換える。次に記事を書くときはここが最初に効く

### 数値の測り方

**Zennのいいね**は curl で取れる。記事HTMLに `"likedCount":N` が埋まっている。

```bash
curl -s -L "https://zenn.dev/yuukikawabata/articles/<zenn.slug>" | grep -oE '"likedCount":[0-9]+'
```

URLは記事の frontmatter の `zenn.slug` から機械的に導ける。**過去記事も全部測れる。**
「URLが残っていないから測れない」は誤り（2026-08-11に一度この判断ミスをした）。

**Xのインプ・いいね**はブラウザで開いて読む。curlでは取れない。

### ステータスコード200を成功の証拠にしない

**x.com も zenn.dev も、存在しないURLに対して200を返す。** どちらもSPAで、
アプリのシェルだけ返してくるため。過去に2回これで誤った判断をしている。

- x.com: 投稿の有無を200で判断してしまった
- zenn.dev: 存在しないslugでも200が返ることを確認済み

**中身で判断する。** Zennなら `likedCount` の有無、Xならブラウザで実際の画面。

### その他

- 計測時刻を必ず記録する。投稿から何時間後かで数字は大きく変わる
- n=1 の結果を確定パターンに昇格させない。SNS側の研究ノートも複数回の再現を待っている
- 測れなかった項目は空のままにする。**推測値を入れない**

## 未整備なもの（正直に書いておく）

- **ブログのPVを計測する手段がない。** Vercel Analytics も Google Analytics も入っていない。
  当面は Zenn のいいねと X のインプを代理指標にする。本気で測るなら計測基盤の導入が先
- **note のスキ数**は手で読むしかない
- **記事経由のブログ流入**が分離できない。X の CTA 投稿のインプが唯一の手がかり

## 定期実行について

このスキルは呼ばれたときにしか動かない。**週次レビューを自動で走らせるには、
ユーザー側のスケジュール設定に登録する必要がある**（SNS側の日次自動化と同じ仕組み）。

登録されていない場合、レビューは記事公開のついでに実行するしかない。
その場合は、公開時に前回公開分の数値を埋める運用にする。
